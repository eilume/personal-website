{% for navitem in nav[locale] %}
	{% if loop.first %}<ul class="flex space-x-4 nav" role="menubar">{% endif %}
    	<li role="menuitem"><a class="text-green-500" href="{{ navitem.url }}">{{ navitem.label }}</a></li>
  	{% if loop.last %}</ul>{% endif %}
{% endfor %}